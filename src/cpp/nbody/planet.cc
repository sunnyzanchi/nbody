#include "planet.h"

namespace nbody {

// TODO: Implement Draw().
void Planet::Draw(/* CanvasRenderingContext2D ctx */) {
 /*
 ctx.beginPath();
 ctx.ellipse(x_, y_, radius_, radius_, 0, 0, 2 * 3.1415);
 ctx.fillStyle = color_;
 ctx.fill();
 */
}

} // namespace nbody
